Welcome to Dusk OS on your m68k Macintosh!

This is a [app/gcon] console. The mouse isn't hooked yet, [drv/mac68k/mouse]
still needs work. In here, the CTRL key is the "command" key, the one with an
apple on it.

Some useful commands:
eject

Here's a few files you might want to edit:
init.fs init.txt
text/ed app/ed
io/stream

The floppy is too tight to hold all tests, but we have those:
f<< tests/kernel.fs rundir<< tests/hal/base
